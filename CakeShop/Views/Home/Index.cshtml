@model HomeViewModel
@await Html.PartialAsync("Slider")
<h2>Prefered Cakes of the week </h2>
<h4>Clients preferences - just for you!</h4>
<div class="row">
    @foreach (var cake in Model.PreferredCakes)
    {
        @Html.Partial("_CakePartial", cake)
    }
</div>